import { ComingSoon } from "@/components/public/coming-soon";

export const metadata = { title: "Entidad · Historia Colombiana" };

export default function EntidadPage() {
  return (
    <ComingSoon
      label="Entidad"
      title="Esta semblanza, pronto"
      note="La página de cada persona, lugar o concepto —con su cronología, relaciones y apariciones— está en construcción."
    />
  );
}
